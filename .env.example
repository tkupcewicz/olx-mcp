# OLX MCP Server Configuration

# Default country code (pl, bg, ro, pt, ua, kz)
OLX_DEFAULT_COUNTRY=pl

# Database path (relative to project root)
OLX_DB_PATH=data/olx-mcp.db

# Rate limiter capacity (default: 4000, max safe: 4500)
OLX_RATE_LIMIT_CAPACITY=4000

# Rate limiter refill window in milliseconds (default: 300000 = 5 min)
OLX_RATE_LIMIT_WINDOW_MS=300000

# Telegram notifications (for price watcher)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# OAuth / Partner API (for offer management via HTTP transport)
# Register at developer.olx.pl to get these credentials
OLX_CLIENT_ID=
OLX_CLIENT_SECRET=
OLX_API_KEY=

# HTTP transport configuration
MCP_SERVER_URL=http://localhost:3000
MCP_HTTP_PORT=3000
